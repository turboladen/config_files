require File.expand_path(File.dirname(__FILE__) + '/spec_helper')
require "<%= file_name %>"

describe <%= klass %> do
  specify { <%= klass %>::VERSION.should == '0.1.0' }

  describe "#initialize" do
    it "should do some stuff" do
      pending "FIXME"
    end
  end
end
